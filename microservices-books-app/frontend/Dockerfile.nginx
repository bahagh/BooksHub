# Simple nginx-only build using pre-built frontend
FROM nginx:alpine

# Copy pre-built files (build folder is in the same directory as this Dockerfile)
COPY ./build /usr/share/nginx/html

# Copy nginx configuration with API proxy
COPY ./nginx.conf /etc/nginx/conf.d/default.conf

# Expose port 80
EXPOSE 80

# Start nginx
CMD ["nginx", "-g", "daemon off;"]
